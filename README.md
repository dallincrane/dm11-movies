### If you clone this make sure to do the following!  

* From the project folder in your terminal run:  
`npm install`

* Add a `/config.json` file with:  
`{ "sessionSecret": "mistbornfordummies" }`
